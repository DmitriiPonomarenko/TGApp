[build]
  command = "npm run build"
  publish = "dist"

# Hash-роутинг: все запросы отдаём index.html (на случай прямых переходов по URL)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
