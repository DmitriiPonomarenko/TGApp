# Supabase (optional). If set, data is synced to Supabase instead of only LocalStorage.
# Где взять: Supabase Dashboard → твой проект → Project Settings (шестерёнка) → вкладка API.
# Project URL и ключ "anon" (public) — скопировать сюда.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Для API напоминаний (Vercel: Settings → Environment Variables).
# SUPABASE_SERVICE_ROLE_KEY — в Supabase: Project Settings → API → service_role (secret).
# TELEGRAM_BOT_TOKEN — токен бота от @BotFather (чтобы бот мог слать сообщения пользователю).
# CRON_SECRET — любой секретный пароль; его передаёт cron при вызове /api/cron/send-reminders.

# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# TELEGRAM_BOT_TOKEN=your-bot-token
# CRON_SECRET=your-cron-secret
